---
id: setup
sidebar_position: 2
title: setup
sidebar_label: setup
---

# Setup

## 1) Requirements

- **SDK Version**: 0.5.0
- **Minimum SDK**: 26 (Android 8.0)
- **Target SDK**: 36
- **Supported ABI**: arm64-v8a, armeabi-v7a

---

### Resources

The SDK requires a video file (`.mp4`) and a face model file (`.npz`).
Place these files in the `assets` (`src/main/assets`) folder, then call `ResourceManager.init()` to copy them to cache and initialize.

```kotlin
import io.klleon.ondevice.ResourceManager

val resourceManager = ResourceManager()

ResourceManager.init(
    context = this,
    mp4FileName = "loop_sample.mp4",
    npzFileName = "loop_sample.npz",
    avatarId = "character_001",
    onSuccess = {
        // Resources ready — you can now call play()
    },
    onError = { error ->
        Log.e("Setup", "Resource preparation failed", error)
    }
)
```

:::info
Internal paths are automatically configured after `init()` completes.
The `onSuccess` callback of `init()` must be called before invoking `play()`.
:::

## 2) Android Configuration

```kotlin
android {
    compileSdk = 36

    defaultConfig {
        minSdk = 26
        targetSdk = 36
    }

    packaging {
        jniLibs {
            pickFirsts += "**/libc++_shared.so"
        }
    }
}
```

## 3) Gradle Dependencies

Add the following dependencies to your app-level `build.gradle.kts`:

```kotlin
dependencies {
    // SDK
    implementation("io.klleon:ondevice-sdk:0.5.0")

    // Coroutines (required by SDK)
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-core:1.9.0")
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.9.0")
}
```

:::info Bundled Libraries
SDK AAR includes the following libraries:
- OkHttp, Okio, Retrofit
- Jackson (Core, Module)
- TensorFlow Lite
- OpenCV
- PyTorch Mobile
- Oboe
- ONNX Runtime
- Agora RTC

If your app uses libraries that depend on OkHttp or Okio (e.g., Coil), version conflicts may occur. In that case, exclude those groups from `RuntimeClasspath`:
```kotlin
configurations.matching { it.name.contains("RuntimeClasspath", ignoreCase = true) }.configureEach {
    exclude(group = "com.squareup.okhttp3")
    exclude(group = "com.squareup.okio")
}
```
:::

## 4) ProGuard / R8

The SDK AAR includes the necessary ProGuard/R8 rules, so **no additional configuration is required.**

If you encounter an `UnsatisfiedLinkError`, check the `jniLibs.pickFirsts` setting in the Android configuration above.
