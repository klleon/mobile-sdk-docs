---
id: examples
sidebar_position: 4
title: android-examples
sidebar_label: android examples
---

# 안드로이드 사용 예제

## MainActivity 구현

```kotlin
// 앱 전역에서 단일 인스턴스를 공유
object SdkConfig {
    val sdk = KlleonOndeviceSdk()
}

class MainActivity : ComponentActivity() {
    private val klleonOndeviceSdk = SdkConfig.sdk
    private val resourceManager = ResourceManager()

    private val requiredPermissions = arrayOf(
        Manifest.permission.RECORD_AUDIO
    )

    private val permissionLauncher = registerForActivityResult(
        ActivityResultContracts.RequestMultiplePermissions()
    ) { permissions ->
        handlePermissionResults(permissions)
    }

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        if (checkPermissions()) {
            initializeApp()
        } else {
            requestPermissions()
        }
    }

    private fun initializeApp() {
        setContent {
            OndeviceSampleTheme {
                Surface(
                    modifier = Modifier.fillMaxSize(),
                    color = MaterialTheme.colorScheme.background
                ) {
                    WorkerScreen(klleonOndeviceSdk = klleonOndeviceSdk)
                }
            }
        }

        // 리소스 초기화 (캐시 복사 + YUV 변환 + 메모리 적재)
        resourceManager.init(
            context = this,
            mp4FileName = "loop_sample.mp4",
            npzFileName = "loop_sample.npz",
            styleFileName = "loop_sample.style",
            avatarId = "character_001",
            onSuccess = {
                // 리소스 준비 완료 — play() 호출 가능
            },
            onError = { error ->
                Log.e("MainActivity", "리소스 준비 실패", error)
            }
        )
    }
}
```

## Compose UI 통합

### WorkerScreen Composable

```kotlin
@Composable
fun WorkerScreen(
    klleonOndeviceSdk: KlleonOndeviceSdk,
    viewModel: WorkerViewModel = viewModel()
) {
    val context = LocalContext.current
    var workerView by remember { mutableStateOf<KlleonSdkView?>(null) }
    var messageData by remember { mutableStateOf("") }

    // SDK 상태 수집
    val accumulatedMessages by viewModel.accumulatedMessages.collectAsState()

    LaunchedEffect(Unit) {
        viewModel.observeSdkState(klleonOndeviceSdk)
    }

    Box(
        modifier = Modifier
            .fillMaxSize()
            .padding(16.dp)
    ) {
        // SDK 상태 표시
        Card(
            modifier = Modifier
                .align(Alignment.TopStart)
                .padding(8.dp),
            colors = CardDefaults.cardColors(
                containerColor = Color.Transparent
            )
        ) {
            Column(modifier = Modifier.padding(12.dp)) {
                Text(
                    text = accumulatedMessages,
                    style = MaterialTheme.typography.bodySmall,
                    color = Color.Black.copy(alpha = 0.8f)
                )
            }
        }

        // 하단 콘텐츠
        Column(
            modifier = Modifier
                .align(Alignment.BottomCenter)
                .fillMaxWidth(),
            verticalArrangement = Arrangement.spacedBy(16.dp)
        ) {
            // SDK 뷰
            Card(
                modifier = Modifier
                    .fillMaxWidth()
                    .height(400.dp)
            ) {
                AndroidView(
                    factory = { ctx ->
                        KlleonSdkView(ctx).also { view ->
                            workerView = view
                        }
                    },
                    modifier = Modifier.fillMaxSize()
                )
            }

            // 메시지 입력
            OutlinedTextField(
                value = messageData,
                onValueChange = { messageData = it },
                label = { Text("메시지") },
                modifier = Modifier.fillMaxWidth()
            )

            // 제어 버튼
            Row(
                modifier = Modifier.fillMaxWidth(),
                horizontalArrangement = Arrangement.spacedBy(8.dp)
            ) {
                Button(
                    onClick = {
                        workerView?.let { view ->
                            CoroutineScope(Dispatchers.IO).launch {
                                klleonOndeviceSdk.play(
                                    context = context,
                                    klleonSdkViewInstance = view,
                                    sdkKey = "YOUR_SDK_KEY",
                                    avatarId = "character_001",
                                    language = "ko_kr"
                                )
                                // 60초 무활동 시 자동 대기화면
                                klleonOndeviceSdk.setIdleTimeout()
                            }
                        }
                    },
                    modifier = Modifier.weight(1f),
                    enabled = workerView != null
                ) {
                    Text("재생")
                }

                Button(
                    onClick = {
                        CoroutineScope(Dispatchers.Default).launch {
                            klleonOndeviceSdk.finish()
                        }
                    },
                    modifier = Modifier.weight(1f)
                ) {
                    Text("종료")
                }
            }

            Row(
                modifier = Modifier.fillMaxWidth(),
                horizontalArrangement = Arrangement.spacedBy(8.dp)
            ) {
                Button(
                    onClick = {
                        klleonOndeviceSdk.sendMessage(messageData)
                    },
                    modifier = Modifier.weight(1f)
                ) {
                    Text("메시지 전송")
                }

                Button(
                    onClick = {
                        klleonOndeviceSdk.sendMessageEcho(messageData)
                    },
                    modifier = Modifier.weight(1f)
                ) {
                    Text("에코")
                }

                Button(
                    onClick = {
                        klleonOndeviceSdk.sendStopSpeech()
                    },
                    modifier = Modifier.weight(1f)
                ) {
                    Text("발화 중단")
                }
            }

            // 음성 인식 버튼
            Row(
                modifier = Modifier.fillMaxWidth(),
                horizontalArrangement = Arrangement.spacedBy(8.dp)
            ) {
                Button(
                    onClick = {
                        if (klleonOndeviceSdk.isVoiceRecognizing()) {
                            klleonOndeviceSdk.stopVoiceRecognition()
                        } else {
                            klleonOndeviceSdk.startVoiceRecognition()
                        }
                    },
                    modifier = Modifier.weight(1f)
                ) {
                    Text("음성 인식")
                }

                Button(
                    onClick = {
                        klleonOndeviceSdk.sendChangeLanguage("en")
                    },
                    modifier = Modifier.weight(1f)
                ) {
                    Text("영어 전환")
                }
            }
        }
    }
}
```

## 대기화면(Standby) 모드

### Wake-word로 대기화면 해제

Wakeword 매칭과 결합하여 음성 명령으로 대기화면을 해제하는 예제입니다.
Wakeword 매칭은 `enterStandby()` 시 자동 활성화, `exitStandby()` 시 자동 비활성화됩니다.

```kotlin
// Wakeword 등록 — 등록만으로는 매칭 비활성 (standby 진입 시 자동 활성화)
klleonOndeviceSdk.registerWakewords(mapOf(
    "ジャパニーズ" to "ja_jp",
    "イングリッシュ" to "en_us",
    "コリアン" to "ko_kr",
))

// Wakeword 감지 시 언어 전환 + standby 해제
LaunchedEffect(Unit) {
    klleonOndeviceSdk.wakewordDetected.collect { result ->
        klleonOndeviceSdk.sendChangeLanguage(result.tag)
        if (klleonOndeviceSdk.isStandby()) {
            klleonOndeviceSdk.exitStandby()  // 매칭도 자동 비활성화
        }
    }
}
```

### STANDBY_ENTER 이벤트 수신

```kotlin
LaunchedEffect(Unit) {
    klleonOndeviceSdk.sdkState.collect { state ->
        if (state.contains("STANDBY_ENTER")) {
            // 대기화면 UI 표시 (오버레이 등)
        }
    }
}
```

### 아바타 응답 상태 모니터링

```kotlin
// StateFlow로 구독
val isResponding by klleonOndeviceSdk.isResponding.collectAsState()

if (isResponding) {
    // 아바타가 응답 준비 중이거나 발화 중
    Text("응답 중...")
}
```

## ViewModel 구현

### WorkerViewModel

```kotlin
class WorkerViewModel : ViewModel() {

    private val _accumulatedMessages = MutableStateFlow("")
    val accumulatedMessages: StateFlow<String> = _accumulatedMessages.asStateFlow()

    fun observeSdkState(klleonOndeviceSdk: KlleonOndeviceSdk) {
        viewModelScope.launch {
            klleonOndeviceSdk.sdkState.collect { state ->
                val messageToAdd = try {
                    val json = JSONObject(state)
                    val chatType = json.optString("chat_type", "")
                    val message = json.getString("message")

                    when (chatType) {
                        "STT_RESULT" -> "[STT] $message"
                        "STT_PARTIAL" -> "[STT 중간] $message"
                        "RESPONSE" -> "[아바타] $message"
                        else -> message
                    }
                } catch (t: Throwable) {
                    state.toString()
                }

                if (messageToAdd.isNotBlank()) {
                    val currentMessages = _accumulatedMessages.value
                    _accumulatedMessages.value = if (currentMessages.isEmpty()) {
                        messageToAdd
                    } else {
                        "$currentMessages\n$messageToAdd"
                    }
                }
            }
        }
    }

    fun clearMessages() {
        _accumulatedMessages.value = ""
    }
}
```

## 최소 통합 예제

간단한 구현을 위해:

```kotlin
@Composable
fun SimpleSDKIntegration() {
    val context = LocalContext.current
    val klleonOndeviceSdk = remember { SdkConfig.sdk }
    var sdkView by remember { mutableStateOf<KlleonSdkView?>(null) }

    Column(
        modifier = Modifier
            .fillMaxSize()
            .padding(16.dp)
    ) {
        // SDK View
        AndroidView(
            factory = { ctx ->
                KlleonSdkView(ctx).also { view ->
                    sdkView = view
                }
            },
            modifier = Modifier
                .fillMaxWidth()
                .weight(1f)
        )

        // 재생 버튼
        Button(
            onClick = {
                sdkView?.let { view ->
                    CoroutineScope(Dispatchers.IO).launch {
                        klleonOndeviceSdk.play(
                            context = context,
                            klleonSdkViewInstance = view,
                            sdkKey = "YOUR_SDK_KEY",
                            avatarId = "character_001",
                            language = "ko"
                        )
                    }
                }
            },
            modifier = Modifier.fillMaxWidth(),
            enabled = sdkView != null
        ) {
            Text("SDK 시작")
        }
    }
}
```

## 아바타 전환 예제

`init()`은 아바타 변경을 자동 감지합니다. 같은 아바타면 버퍼를 즉시 재사용하고, 다른 아바타면 이전 버퍼를 해제 후 새로 할당합니다.

```kotlin
class CharacterSelectActivity : ComponentActivity() {

    fun onAvatarSelected(avatarMp4: String, avatarNpz: String, avatarId: String) {
        // init()이 avatarId를 비교하여 자동으로 버퍼 재사용/전환 처리
        // 같은 아바타: 즉시 onSuccess (0ms)
        // 다른 아바타: 이전 버퍼 해제 + 새 할당
        ResourceManager.init(
            context = this,
            mp4FileName = avatarMp4,
            npzFileName = avatarNpz,
            avatarId = avatarId,
            onSuccess = {
                // 리소스 준비 완료 — ChatActivity로 이동
                startActivity(Intent(this, ChatActivity::class.java))
            },
            onError = { error ->
                Log.e("CharacterSelect", "리소스 준비 실패", error)
            }
        )
    }

    override fun onDestroy() {
        super.onDestroy()
        // play() 전 이탈 시 메모리 해제
        if (!KlleonOndeviceSdk.playing) {
            ResourceManager.deinit()
        }
    }
}
```

## 상태 관찰 패턴

```kotlin
@Composable
fun SDKStateObserver(klleonOndeviceSdk: KlleonOndeviceSdk) {
    var currentState by remember { mutableStateOf("") }

    LaunchedEffect(klleonOndeviceSdk) {
        klleonOndeviceSdk.sdkState.collect { state ->
            currentState = try {
                val json = JSONObject(state)
                "상태: ${json.optString("chat_type")} - ${json.getString("message")}"
            } catch (e: Exception) {
                "원시 상태: $state"
            }
        }
    }

    Text(
        text = currentState,
        modifier = Modifier.padding(16.dp)
    )
}
```

## Wakeword 기반 언어 전환 (Standby 연동)

대기화면(Standby) 모드에서 음성 wakeword를 감지하여 아바타의 대화 언어를 자동 전환하고 대기화면을 해제하는 예제입니다.

:::info STT 언어와 wakeword 등록 전략
`play()` 또는 `sendChangeLanguage()`에 전달한 언어가 STT 인식 언어로도 설정됩니다. Wakeword는 **현재 STT 언어가 인식할 수 있는 단어**로 등록해야 합니다.

예를 들어 한국어 모드(`ko_kr`)에서는 `"니혼고"`, `"잉글리쉬"` 같은 한국어 발음 wakeword가 인식되고, 일본어 모드(`ja_jp`)로 전환되면 `"韓国語"`, `"英語"` 같은 일본어 wakeword가 인식됩니다.

따라서 **모든 지원 언어의 wakeword를 한꺼번에 등록**하는 것이 좋습니다.
:::

:::caution Standby 모드 필수
Wakeword 매칭은 기본적으로 비활성화 상태이며, `enterStandby()` 진입 시 자동으로 활성화됩니다. `setIdleTimeout()`으로 자동 대기화면을 설정하거나, 수동으로 `enterStandby()`를 호출해야 wakeword 매칭이 동작합니다.
:::

```kotlin
@Composable
fun WakewordLanguageSwitcher(klleonOndeviceSdk: KlleonOndeviceSdk) {
    var currentLanguage by remember { mutableStateOf("ko_kr") }

    // 모든 언어에서 인식 가능한 wakeword를 한꺼번에 등록
    LaunchedEffect(Unit) {
        klleonOndeviceSdk.registerWakewords(mapOf(
            // 한국어 STT에서 인식
            "니혼고" to "ja_jp", "일본어" to "ja_jp",
            "잉글리쉬" to "en_us", "영어" to "en_us",
            // 일본어 STT에서 인식
            "韓国語" to "ko_kr", "コリアン" to "ko_kr",
            "英語" to "en_us",
            // 영어 STT에서 인식
            "Korean" to "ko_kr",
            "Japanese" to "ja_jp",
        ))

        // 자동 대기화면 활성화 — standby 진입 시 wakeword 매칭도 자동 활성화
        klleonOndeviceSdk.setIdleTimeout(60_000L)
    }

    // Wakeword 감지 시 언어 전환 + standby 해제
    LaunchedEffect(Unit) {
        klleonOndeviceSdk.wakewordDetected.collect { result ->
            klleonOndeviceSdk.sendChangeLanguage(result.tag)
            currentLanguage = result.tag
            if (klleonOndeviceSdk.isStandby()) {
                klleonOndeviceSdk.exitStandby()  // 매칭도 자동 비활성화
            }
        }
    }

    Text(
        text = "현재 언어: $currentLanguage",
        modifier = Modifier.padding(16.dp)
    )
}
```
